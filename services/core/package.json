{
  "name": "growthop-core-api",
  "version": "0.1.0",
  "private": true,
  "description": "Core API: auth, RBAC, LMS CRUD, and the AI chat proxy.",
  "scripts": {
    "build": "nest build",
    "start": "nest start",
    "start:dev": "nest start --watch",
    "prisma:generate": "prisma generate --schema ../../prisma/schema.prisma"
  },
  "dependencies": {
    "@nestjs/common": "^10.4.0",
    "@nestjs/core": "^10.4.0",
    "@nestjs/platform-express": "^10.4.0",
    "@prisma/client": "^5.20.0",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.1"
  },
  "devDependencies": {
    "@nestjs/cli": "^10.4.0",
    "prisma": "^5.20.0",
    "typescript": "^5.6.0"
  }
}
